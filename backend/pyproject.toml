[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "venknag1@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "neo4j>=6.3.1",
    "python-dotenv>=1.2.3",
]

[project.scripts]
backend = "backend:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
